import { Typography } from '@mui/material';
import React from 'react';

const AddProducts = () => {
    return (
        <div>
            <Typography id="btn" variant="h6">Add Products</Typography>
        </div>
    );
};

export default AddProducts;